import { boot } from 'quasar/wrappers';
/*
import moment from 'moment';

const logTime = function(test : ,msg : string) {
  const ora = moment()
  const delta = ora - test.log.clientTime
  test.log.serverTime = test.log.serverTime.add(delta,'ms')
  test.log.clientTime = ora
  test.log.STORIA.push(test.log.serverTime.format("LTS") + " = " + msg)
  return moment(test.log.serverTime)
}
*/

export default boot(async () => {
  /* { app, router, ... } */
});

// export ( logTime )
